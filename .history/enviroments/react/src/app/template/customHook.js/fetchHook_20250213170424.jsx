const { useState } = require("react");

const useFetch = (url) => {
  const [data, setData] = useState("");
  const [isLoading, setisLoading] = useState(false);
  const [isLoading, setisLoading] = useState(false);
};
